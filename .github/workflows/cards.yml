name: Update README cards
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: stats
          options: username=${{ github.repository_owner }}&show_icons=true&count_private=true&include_all_commits=true
          path: profile/stats.svg
          token: ${{ secrets.GH_PAT }}

      - name: Generate pin card
        uses: readme-tools/github-readme-stats-action@v1
        with:
          card: pin
          options: username=readme-tools&repo=github-readme-stats
          path: profile/pin-readme-tools-github-readme-stats.svg
          token: ${{ secrets.GH_PAT }}

      - name: Commit cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/*.svg
          git commit -m "Update README cards" || exit 0
          git push

      - name: Generate metrics card
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GH_PAT }}
          filename: profile/metrics.svg
          base: ""
          config_order: languages, lines, stars, habits
          plugin_languages: yes
          plugin_languages_indepth: yes
          plugin_languages_sections: most-used
          plugin_languages_details: bytes-size, percentage, lines
          plugin_languages_limit: 0
          plugin_languages_threshold: 0%
          plugin_languages_ignored: postscript, svg
          plugin_languages_other: no
          plugin_languages_colors: github
          plugin_languages_categories: data, markup, programming, prose
          plugin_languages_recent_categories: data, markup, programming, prose
          plugin_languages_recent_load: 1000
          plugin_languages_recent_days: 90
          plugin_languages_analysis_timeout: 60
          plugin_languages_analysis_timeout_repositories: 15
          repositories: 1000
          repositories_affiliations: owner
          plugin_languages_indepth_custom: hazelgrove/hazel@asymmetric-unknown-type, hazelgrove/hazel@type-slicing, Extreme-Kayaking/kayak-hull-optimisation, Talking-Music/Talking-Music
          commits_authoring: ${{ github.repository_owner }}, 133233123+MaxCarroll0@users.noreply.github.com, mjvcarroll@gmail.com, mjvcaroll@gmail.com
